for (int i = 0; i < directionalLightCount; i++)
{
  float dot = max(0.0, (gl_FrontFacing ? 1.0 : -1.0)*dot(directionalLightDir[i], Normal));
  diffuseLight += directionalLightColor[i]*dot;
  float hilightDot = (dot == 0.0 ? 0.0 : abs(dot(directionalLightHilightDir[i], Normal)));
  hilightLight += pow(hilightDot, (1.0-roughness)*128.0+1.0);
}
